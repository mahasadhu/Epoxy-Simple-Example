ext {

    versions = [
            kotlin: "1.4.31",
            androidx_core: "1.3.2",
            androidx_appcompat: "1.2.0",
            material: "1.3.0",
            constraintlayout: "2.0.4",

            epoxy: "4.6.1",

            glide: "4.12.0"
    ]

    kotlin = [
            stdlib: "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}"
    ]

    androidx = [
            core: "androidx.core:core-ktx:${versions.androidx_core}",
            appcompat: "androidx.appcompat:appcompat:${versions.androidx_appcompat}",
            constraintlayout: "androidx.constraintlayout:constraintlayout:${versions.constraintlayout}"
    ]

    views = [
            material: "com.google.android.material:material:${versions.material}"
    ]

    airbnb = [
            epoxy: "com.airbnb.android:epoxy:${versions.epoxy}",
            epoxy_annotation: "com.airbnb.android:epoxy-processor:${versions.epoxy}"
    ]

    glide = [
            lib: "com.github.bumptech.glide:glide:${versions.glide}",
            annotation: "com.github.bumptech.glide:compiler:${versions.glide}"
    ]

}